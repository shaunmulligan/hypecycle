<!-- Currently not used -->
<!-- <ion-item>
    <ion-icon slot="start" name={icon}>
    </ion-icon>
    <ion-label>
        <h2>{name}</h2>
        <h3>{address}</h3>
    </ion-label>
    <ion-checkbox
    value=address
    slot="end"
    color="primary"
    checked
    on:ionChange={checkBoxChange} />
</ion-item>

<script>
    export let name = "UKNOWN"
    export let icon = "alert"
    // export let protocol = "--"
    export let address = "24:71:89:cc:09:05"
    // Add battery level and connectivity status
    const checkBoxChange = event => {
        if (event.detail.value) {
        characters[event.detail.value] = event.detail.checked;
        console.log(JSON.stringify(characters, null, 2));
        }
    };
</script> -->